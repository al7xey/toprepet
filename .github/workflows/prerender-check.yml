name: Check prerender build

on:
  push:
    branches:
      - geo-prerender
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build with prerender
        run: pnpm run build

      - name: Verify prerendered HTML
        run: |
          grep -Fq "<h1" dist/index.html
          grep -Fq "1 200 ₽" dist/index.html

          if grep -Fq '<div id="root"></div>' dist/index.html; then
            echo "Ошибка: root остался пустым"
            exit 1
          fi

          echo "Пререндеринг подтверждён: HTML содержит контент TopRepet."
      - name: Upload preview
        uses: actions/upload-artifact@v4
        with:
          name: toprepet-prerender-preview
          path: dist
